# Load at init the keys

    Code
      aemet_daily_clim()
    Condition
      Error in `get_data_aemet()`:
      ! API key can't be missing. See `climaemet::aemet_api_key()`.

---

    Code
      aemet_daily_clim(extract_metadata = TRUE)
    Condition
      Error in `get_metadata_aemet()`:
      ! API key can't be missing. See `climaemet::aemet_api_key()`.

